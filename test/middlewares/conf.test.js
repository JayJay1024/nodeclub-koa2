'use strict';

const conf   = require('../../middlewares/conf');
const config = require('../../config');
const should = require('should');

describe('test/middlewares/conf.test.js', function () {
    it('should alert no github oauth');
});
