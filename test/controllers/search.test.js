'use strict';

const app     = require('../../app');
const request = require('supertest')(app);
const should  = require('should');

describe('test/controllers/search.test.js', function () {
    it('should redirect to google search');
});
